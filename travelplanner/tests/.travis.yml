# language: python
# python:
#   - "3.7"
# # command to install dependencies
# install:
#   - pip install -r requirements.txt
# script:
#   # - cd travelplanner  
#   - pytest 
language: python
python:
  - "3.7"
  # - "3.8"
# command to install dependencies
before_install:
  - cd mphy0021-2019-travel-planner-MIGUEL-LO/travelplanner/tests
install:
  # install dependencies for testing
  - pip install pytest pytest-cov
  # install the package
  - pip install .
# command to run tests
script:
  - pytest --cov=./